# This file is documentation only — copy it if you want to run the fetch
# script locally, but never commit a file containing a real key.
#
# In GitHub Actions, this value comes from the OMDB_API_KEY repository
# secret instead of a file, so production never needs a .env at all.

OMDB_API_KEY=your_omdb_api_key_here
